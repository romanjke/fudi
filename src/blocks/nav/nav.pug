mixin nav()
    - var items = ['home', 'about', 'app', 'testimonials', 'cuisines']

    nav.nav#menu
        ul.nav__list
            each item in items
                li.nav__item
                    a(href="#" + item).nav__link= item